{
  "name": "material-solutions-app-railway-entry",
  "version": "1.0.0",
  "private": true,
  "description": "Railway root entrypoint for the Material Solutions FSM backend.",
  "scripts": {
    "start": "node backend/server.js",
    "test": "npm --prefix backend test",
    "check:fsm-scripts": "node --check scripts/check-fsm-pr-readiness.js && node --check scripts/smoke-admin-session.js && node --check scripts/smoke-fsm-live.js && node --check scripts/assert-fsm-readiness-contract.js && node scripts/assert-fsm-readiness-contract.js",
    "check:fsm-pr-readiness": "node scripts/check-fsm-pr-readiness.js",
    "check:fsm-pr-readiness:json": "node scripts/check-fsm-pr-readiness.js --json",
    "smoke:admin-session": "node scripts/smoke-admin-session.js",
    "smoke:fsm-live": "node scripts/smoke-fsm-live.js",
    "railway:build": "npm ci --prefix backend --omit=dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
